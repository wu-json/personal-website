export type Construct = {
  id: string;
  title: string;
  subtitle: string;
  date: string;
  cover: string;
  coverWidth: number;
  coverHeight: number;
  body: string;
  coverPosition?: string;
  linkLabel?: string;
  link?: string;
};
